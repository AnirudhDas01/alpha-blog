 <nav class="navbar navbar-expand-lg bg-body-tertiary">
      <div class="container-fluid">
      <% if logged_in? %>
            <%= link_to "ALPHA BLOG", articles_path, class: "navbar-brand" , id:"logo" %>
        <% else %>
         <%= link_to "ALPHA BLOG", root_path, class: "navbar-brand" , id:"logo" %>
        <% end %>

        <div class="collapse navbar-collapse" id="navbarSupportedContent">
          <ul class="navbar-nav me-auto mb-2 mb-lg-0">
            <li class="nav-item">
              <%= link_to "Articles", articles_path, class:"nav-link active" %>
            </li>

            <% if logged_in? %>
              <li class="nav-item">
                <%= link_to "Bloggers", users_path, class:"nav-link active" %>
              </li>
              <li class="nav-item">
                <%= link_to current_user.username ,user_path(current_user), class:"nav-link active" %>
              </li>
            <li class="nav-item">
                <%= link_to "Create Article", new_article_path, class:"nav-link active" %>
            </li>
            <li class="nav-item">
                <%= link_to "Log Out", logout_path, class:"nav-link active", data: {turbo_method: :delete} %>
            </li>
            <% else %>
              <li class="nav-item ">
                <%= link_to "Log In", login_path, class:"nav-link active" %>
              </li>
              <li class="nav-item">
              <%= link_to "Sign up", signup_path, class:"nav-link active" %>
              </li>
            <% end %>
          </ul>
        </div>
      </div>
    </nav>