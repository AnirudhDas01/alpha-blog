<<<<<<< HEAD
<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Article Details</title>
    <style>
        body {
            font-family: Arial, sans-serif;
            background-color: #f4f4f4;
            margin: 0;
            padding: 20px;
        }

        h1 {
            color: #333;
            text-align: center;
        }

        .article-details {
            max-width: 600px;
            margin: 20px auto;
            padding: 20px;
            background-color: #fff;
            box-shadow: 0 0 10px rgba(0, 0, 0, 0.1);
            border-radius: 10px;
        }

        .article-details p {
            font-size: 18px;
            line-height: 1.6;
            margin-bottom: 20px;
        }

        .article-details p strong {
            color: #555;
        }

        .actions {
            display: flex;
            justify-content: space-between;
            flex-wrap: wrap;
            gap: 10px;
        }

        .actions a {
            padding: 10px 20px;
            text-decoration: none;
            color: #fff;
            background-color: #007BFF;
            border-radius: 5px;
            transition: background-color 0.3s ease;
            text-align: center;
            flex: 1;
        }

        .actions a:hover {
            background-color: #0056b3;
        }

        .actions .delete {
            background-color: #DC3545;
        }

        .actions .delete:hover {
            background-color: #c82333;
        }

        .actions .show-all {
            background-color: #28A745;
        }

        .actions .show-all:hover {
            background-color: #218838;
        }
    </style>
</head>
<body>
    <h1>Showing Article Details</h1>
    <div class="article-details">
        <p><strong>Title:</strong> <%= @article.title %></p>
        <p><strong>Description:</strong> <%= @article.description %></p>
        <div class="actions">
            <%= link_to 'Edit', edit_article_path(@article) %>
            <%= link_to 'Delete', article_path(@article), data: {turbo_confirm: "Are you sure.?", turbo_method: :delete }, class: 'delete' %>
            <%= link_to 'Show All Articles', articles_path, class: 'show-all' %>
        </div>
    </div>
</body>
</html>
=======
<h1>Showing article details</h1>
<p><Strong>Title: </Strong><%= @article.title %></p>
<p><Strong>Description: </Strong><%= @article.description %></p>

                     <td><%= link_to 'Go back', articles_path %></td>
                <td><%= link_to 'Edit', edit_article_path(@article) %></td>
                <td><%= link_to 'Delete' , article_path(@article) ,data: {turbo_method: :delete , turbo_confirm: "Are you sure?"} %></td>
>>>>>>> udemy_follow_up
