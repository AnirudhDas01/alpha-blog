<h1 class="text-center"> <%= @user.username.capitalize%>'s Profile</h1>
<div class="container text-center">



<%= gravatar_for( @user, size: 250) %>
<%= link_to "Edit #{@user.username.capitalize}'s Profile", edit_user_path(@user),  class:"btn btn-outline-info mt-4" %>

<div>


<h3 class="text-center mt-4 "> <%= @user.username.capitalize%>'s Articles</h3>

<div class = "container">

<div class="flickr_pagination mb-4"> 
    <%= will_paginate @articles, :container => false %>
</div>

   <%= render 'articles' %>

</div>